import { CommentformDirective } from './commentform.directive';

describe('CommentformDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentformDirective();
    expect(directive).toBeTruthy();
  });
});
